Fixes from tester feedback — thank you!

- Fixed the layout mirroring on right-to-left / non-Latin device locales (e.g. Arabic): the interface now always displays left-to-right, so toggles and text line up correctly.
