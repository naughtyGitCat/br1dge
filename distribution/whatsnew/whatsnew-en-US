Fixes from tester feedback — thank you!

- The first-run consent screen now scrolls correctly on small screens, and the agree button is always visible.
- "Test send" now explains that a delivery channel must be configured first, instead of failing silently.
